<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link rel="icon" type="image/svg+xml" href="/student-favicon.svg" />
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <%- include('../partials/header') %>

  <main class="main-content">
    <div class="saved-jobs-container">
      <a href="/jobs" class="back-link">← Back to All Jobs</a>

      <h1>Saved Jobs</h1>

      <% if (jobs.length > 0) { %>
        <div class="jobs-list">
          <% jobs.forEach(job => { %>
            <div class="job-card">
              <div class="job-header">
                <h3><a href="/jobs/job/<%= job._id %>"><%= job.title %></a></h3>
                <span class="job-type"><%= job.jobType %></span>
              </div>
              <p class="company"><strong><%= job.company %></strong></p>
              <p class="location">📍 <%= job.location %></p>
              <p class="salary"><strong>Salary:</strong> <%= job.salary %></p>
              <div class="job-actions">
                <a href="/jobs/job/<%= job._id %>" class="btn btn-secondary btn-sm">View Details</a>
                <a href="<%= job.applicationLink %>" target="_blank" rel="noopener noreferrer" class="btn btn-primary btn-sm">Apply Now</a>
              </div>
            </div>
          <% }); %>
        </div>
      <% } else { %>
        <div class="no-saved">
          <p>You haven't saved any jobs yet.</p>
          <a href="/jobs" class="btn btn-primary">Browse Jobs</a>
        </div>
      <% } %>
    </div>
  </main>

  <%- include('../partials/footer') %>

  <script src="/js/main.js"></script>
</body>
</html>
